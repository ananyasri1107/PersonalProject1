[project]
name = "personalproject1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sagarsrivastava021-sketch", email = "sagarsrivastava021@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "flask-login>=0.6.3",
    "flask-sqlalchemy>=3.1.1",
]

[project.scripts]
personalproject1 = "personalproject1:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
